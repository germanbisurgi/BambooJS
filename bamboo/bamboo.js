var Bamboo = {
    game: Game,
    loop: Loop,
    loader: Loader,
    screen: Screen,
    inputs: Inputs,
    renderer: Renderer,
    state: State,
    entityManager: EntityManager,
    timeManager: TimeManager,
    cameraManager: CameraManager,
    entity: Entity,
    camera: Camera
};