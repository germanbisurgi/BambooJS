var Bamboo = {
    game: Game,
    loop: Loop,
    assets: Assets,
    screen: Screen,
    inputs: Inputs,
    renderer: Renderer,
    state: State,
    entityManager: EntityManager,
    timeManager: TimeManager,
    cameraManager: CameraManager,
    entity: Entity,
    texture: Texture,
    camera: Camera,
    sprite: Sprite,
    tileSprite: TileSprite
};